# Binette

Binette is a compact binary value format with schemas, stable type identities,
compatibility tooling, and support for long-lived data.

The project is being split out of Vox so the value format, schema model,
schema bundles, compatibility checks, and translation planning can evolve as a
standalone substrate for RPC, storage, fixtures, and archives.
